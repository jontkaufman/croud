# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
venv/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Generated review screenshots (large; tracked separately if desired)
# Comment out the next line to track screenshots in git
reviews/**/screenshots/

# Local secrets / scratch
.env
scratch/
.gstack/

# Personal archives (excluded from public repo)
# Place any persona files, review runs, or dev artifacts you want to keep locally but not publish here.
personas-archive-*/
reviews-archive-*/
docs-archive-*/

# Local git backups (safety nets created during repo cleanup)
.git-backup-*/

# Persona subfolders ("groups"). Public repo ships with personas/ empty
# except _index.md; users organize their own libraries in subfolders.
# Each user's groups are their own — gitignored by default. Comment out
# any line below if you intentionally want to share a group publicly.
personas/*/

# Local Claude/Codex settings — keep your own keys/permissions out of the public repo
.claude/settings.local.json
